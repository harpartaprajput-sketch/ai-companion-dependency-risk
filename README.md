# ai-companion-dependency-risk
ML model predicting AI-companion dependency risk from synthetic user behavior data — trained and compared across Random Forest, Gradient Boosting, SVM, and XGBoost, with GridSearchCV tuning.
